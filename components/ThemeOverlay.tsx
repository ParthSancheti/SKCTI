"use client";

/* R2 — clip-path circle overlay used by store.toggleTheme() */
export default function ThemeOverlay() {
  return <div id="theme-overlay" className="no-transition" aria-hidden="true" />;
}
